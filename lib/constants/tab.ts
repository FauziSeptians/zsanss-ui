export enum SIZE {
  FULL = "full",
  LG = "lg",
  MD = "md",
  SM = "sm",
}

export enum VARIANT_TAB {
  UNDERLINE = "underline",
  DEFAULT = "default",
  BORDERED = "bordered",
}

export enum COLOR { 
    PRIMARY = "primary",
    SECONDARY = "secondary",
    NEUTRAL = "neutral"
}
